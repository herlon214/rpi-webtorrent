const defaultState = {
  user: 'transmission',
  password: 'transmission',
  token: ''
}

export default function (state = defaultState, action) {
  switch (action.type) {

    default: return state
  }
}